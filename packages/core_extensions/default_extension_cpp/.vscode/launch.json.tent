{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "standalone test (lldb, launch)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bin/{{package_name}}_test",
      "args": [
        // "--gtest_filter=*",
      ],
      "cwd": "${workspaceFolder}",
    }
  ]
}