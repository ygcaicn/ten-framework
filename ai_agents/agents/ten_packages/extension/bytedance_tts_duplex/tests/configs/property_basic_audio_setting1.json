{
    "appid": "${env:BYTEDANCE_TTS_APPID}",
    "token": "${env:BYTEDANCE_TTS_KEY}",
    "dump": true,
    "dump_path": "./tests/keep_dump_output/",
    "params": {
        "audio_params": {
            "sample_rate": 16000
        }
    }
}